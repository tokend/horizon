/v3/accounts/{id}:
  get:
    tags:
      - Accounts
    summary: Get Account by ID
    description: |
                  Allows to get `Account` with corresponding details.
    operationId: getAccountByID
    parameters:
    - name: id
      in: path
      description: Unique identifier of account
      required: true
      style: simple
      explode: false
      schema:
        type: string
    - name: include
      in: query
      description: |
        🔒 The following relationships require an owner's or admin's signature to be included:
        * `balances.state`
        * `referrer`
        * `fees`
        * `role`
        * `role.rules`
        * `limits`
        * `external_system_ids`
      required: false
      style: form
      explode: false
      schema:
        type: string
        enum:
          - fees
          - balances
          - balances.asset
          - balances.state
          - referrer
          - limits
          - external_system_ids
        example: balances,balances.asset
    responses:
      200:
        description: account exists
        content:
          application/vnd.api+json:
            schema:
              type: object
              required:
                - data
                - included
              properties:
                data: 
                  type: object
                  $ref: '#/components/schemas/Account'
                included:
                  $ref: '#/components/schemas/Resources'
      400:
        description: bad input parameter
        content:
          application/vnd.api+json:
            schema:
              $ref: '#/components/schemas/Errors'
      401:
        description: invalid signature is provided or a signer is not authorized to access specified resources
        content:
          application/vnd.api+json:
            schema:
              $ref: '#/components/schemas/Errors'
      404:
        description: such an account does not exist
        content:
          application/vnd.api+json:
            schema:
              $ref: '#/components/schemas/Errors'
