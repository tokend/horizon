AssetKey:
  type: object
  required:
      - id
      - type
  properties:
      id:
        type: string
        example: "USD"
      type:
        type: string
        enum: 
          - "assets"
Asset:
  allOf:
  - $ref: '#/components/schemas/AssetKey'
  - type: object
    properties:
      relationships:
        type: object
        properties:
          owner:
            type: object
            description: "Owner of the asset"
            properties:
              data:
                type: object
                $ref: '#/components/schemas/AccountKey'
      attributes:
        type: object
        properties:
          pre_issuance_asset_signer:
            type: string
            description: "address of signer responsible for pre issuance. [Details↪](https://tokend.gitbook.io/knowledge-base/technical-details/key-entities/asset#pre-issued-asset-signer)"
            example: "GBSR6JG5AYSAW7HK6EGJFYVIVN54LVGSY3ZLJ6X3IBQZ766EJABCZQTH"
          max_issuance_amount:
            type: string
            description: "Max volume of asset which can be in circulation"
            example: "1000.000001"
          available_for_issuance:
            type: string
            description: "Volume of asset authorized to be issued by asset owner"
            example: "20.000000"
          issued:
            type: string
            description: "Volume of asset currently in circulation"
            example: "10.000000"
          pending_issuance:
            type: string
            description: "Volume of asset locked by the system to be distributed via [asset sale↪](https://tokend.gitbook.io/knowledge-base/platform-features/crowdfunding)"
            example: "123.000123"
          policies:
            type: object
            decription: "Defines properties of the asset"
            $ref: '#/components/schemas/Mask'
          trailing_digits:
            type: integer
            description: "Number of significat digits after point"
            maximum: 6
            minimum: 0
            example: 2
          details: 
            type: object
            description: "free-form object to attach asset details like link to icon, short description, etc."